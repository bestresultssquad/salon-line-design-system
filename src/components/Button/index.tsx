import { Text } from 'react-native';
import { Container } from './Button.styles';

const Button = () => {
  return (
    <Container>
      <Text>Click me</Text>
    </Container>
  );
};

export default Button;
